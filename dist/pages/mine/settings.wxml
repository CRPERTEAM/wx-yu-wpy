
  <view class="mine-settings">
    <view class="section">
      <view class="title">重新授权</view>
      <view class="list">
        <view class="item"  wx:for="{{items}}" wx:for-key="index" wx:for-index="index" wx:key="index" bindtap="authroize" data-wpyauthroize-a="{{item.type}}">
          {{ item.title }}
          <view class="can-jump"/>
        </view>
      </view>
    </view>
  </view>